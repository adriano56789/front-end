// Hook de zoom removido - API não existe mais
export const useZoomSettings = (userId?: string) => {
    // Zoom desabilitado - sempre usa 100%
    return;
};

export const resetZoom = () => {  
    // Reset de zoom desabilitado
    return;
};
