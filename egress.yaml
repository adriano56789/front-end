# LiveKit Egress Configuration
# Connects to LiveKit server and SRS via internal Docker network
# Backend API triggers RoomCompositeEgress per stream via LiveKit API

api_key: dfd8670dd9bc2f158eb5da850fcb47778f19fc9c88ed8f76f2d1d34cd0d29c66
api_secret: 8529e52ace6e923af0a1b72dffe29fc59372faa96b302c398c9142e197f7636d

# Internal WebSocket URL (LiveKit on host network, reachable via host.docker.internal)
ws_url: ws://host.docker.internal:7880

# Same Redis used by LiveKit server (accessible via livego-net network)
redis:
  address: redis:6379
